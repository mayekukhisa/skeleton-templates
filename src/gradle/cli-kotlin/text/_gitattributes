# All text files should be normalized (line endings converted to LF)
* text=auto

# Windows files should use CRLF line endings
*.bat text eol=crlf

# Binary files should be left untouched
*.jar binary
